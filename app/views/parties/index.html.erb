<h1>Listing parties</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Award</th>
    <th>Limitvolumeteam</th>
    <th>Limitdateregistration</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @parties.each do |party| %>
  <tr>
    <td><%= party.name %></td>
    <td><%= party.award %></td>
    <td><%= party.limitvolumeteam %></td>
    <td><%= party.limitdateregistration %></td>
    <td><%= link_to 'Show', party %></td>
    <td><%= link_to 'Edit', edit_party_path(party) %></td>
    <td><%= link_to 'Destroy', party, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Party', new_party_path %>
